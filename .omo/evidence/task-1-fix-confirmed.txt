
LineNumber Line
---------- ----
       187         sys.exit(0 if ok else 1)
       191         sys.exit(0 if ok else 1)
       214         sys.exit(1)
       262             sys.exit(0)
       292         sys.exit(0)
       305         os._exit(0)
       338         # 登录成功会 sys.exit(0)，不会到达这里；失败超限则回退到正常启动
       354                 on_exit=lambda: os.kill(os.getpid(), signal.SIGTERM) if hasattr(signal, "SIGTERM") else os._…

